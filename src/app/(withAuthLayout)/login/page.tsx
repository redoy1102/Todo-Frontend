import Login from '@/components/clients/Login.client';

const LoginPage = () => {
  return (
    <div className="h-screen flex justify-center items-center flex-col">
      <Login />
    </div>
  );
};

export default LoginPage;
