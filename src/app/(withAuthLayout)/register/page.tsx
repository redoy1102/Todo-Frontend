import Register from '@/components/clients/Register.client';

const RegisterPage = () => {
  return (
    <div className="h-screen flex justify-center items-center flex-col">
      <Register />
    </div>
  );
};

export default RegisterPage;
